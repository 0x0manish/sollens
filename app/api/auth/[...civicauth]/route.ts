import { handler } from '@civic/auth-web3/nextjs'

// In Next.js Route Handlers, you need to export functions directly, not with object property assignment
export const GET = handler()
export const POST = handler() 